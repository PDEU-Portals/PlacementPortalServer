const express = require('express');
const router = express.Router();
const User = require('../models/user');
//User is name of schema defined so far

//define various requests as you see fit. 
//Will define my end of requests shortly